# 準モンテカルロ法における目標精度

本稿の主張は，準モンテカルロ法(QMC)においては$\|F^{-1} - r\|_{\infty} \sim 10^{-7}$ であればよい，ということである．簡単のため1次元の場合を考える．

$X$を1次元の確率変数とし, $g(X)$ の期待値計算を考える. $g(x)$は, 例えばペイオフ関数などに相当する. $f(x)$， $F(x)$をそれぞれ$X$の密度関数，累積密度関数とする．
このとき$E := E[g(X)] =\int_{\mathbb{R}}g(x)f(x)dx$ は期待値計算における「真の積分値」である. 
ここで，$\frac{d}{du}F^{-1}(u) = \frac{1}{f(F^{-1}(u))}$ であることから，$x = F^{-1}(u)$ のとき,  $dx = \frac{1}{f(x)} du$ が成り立つ．よって
$E=E[g(X)] =\int_{\mathbb{R}}g(x)f(x)dx = \int_{[0,1]}g(F^{-1}(u))du\quad (=\int_{[0,1]}(g \circ F^{-1})(u)du)$ 
が成り立つ.  

通常のモンテカルロ法(MC)を考える．MCの試行回数(e.g. パス数)を$N$とし，$\{X_{i}\}_{1 \le i \le N }$ を$X$ と同じ分布に従う独立な確率変数列とする．また，$\{U_{i}\}_{1 \le i \le N }$ を$U(0,1)$ に従う独立な一様乱数列とする． このとき
$E_{N} := \frac{1}{N}\sum_{i=1}^{N} g(X_{i}) = \frac{1}{N}\sum_{i=1}^{N} g(F^{-1}(U_{i}))$ 
は確率変数であり，MCの「真の平均値」である．
次に，$r(u)$ を$F^{-1}(u)$ の有理関数近似式とする．すると
$\hat{E}_{N}(r):=\frac{1}{N}\sum_{i=1}^{N} g(r(U_{i}))$ 
は確率変数であり，MCの「近似的な平均値」である．

MCとは，大数の法則を使って， $E$ の値を$E_{N}$ で近似することを考えるわけである．$g\circ F^{-1} \in \mathcal{L}^2([0,1],\mathcal{B}([0,1]),\text{Leb})$ならば，重複対数の法則より，任意の$\epsilon > 0$ に対してある$N_{0} \in \mathbb{N}$ が存在して$N > \mathbb{N}_{\ge N_{0}}$ に対して
$$
|E_{N}(\omega) - E| < (1 + \epsilon)\sqrt{\frac{\sigma^2 \log \log N}{N} } = \text{ほぼ} \mathcal{O}(N^{-1/2}) \quad a.s. \omega
$$
一方，準モンテカルロ法(QMC)では，準乱数列と呼ばれる数列を使って一様乱数の列$\{U_{i}\}_{1 \le i \le N }$を再現するが，準乱数列では独立性条件が破れるため，大数の法則の代わりに，独立性条件を外しても成り立つ「準・大数の法則」のようなものを使って$E$ の値を$E_{N}$ で近似することを考える．結論からいうと，Sobol’列などの上手い準乱数列を持ってくると
$$
|E_{N}(\omega) - E| < C \frac{ \log N }{N} \text{あるいは} C \frac{ 1 }{N} = \text{ほぼ} \mathcal{O}(N^{-1})  \quad a.s. \omega
$$
とできる．($C$は$g \circ F^{-1}$ の「お行儀のよさ」などによって決まる定数)
ここまでは、真の積分値$E$に対する「厳密なMC/QMC平均値$E_{N}$」の誤差オーダーの話である．この誤差を「モンテカルロの誤差」「サンプリング誤差」「統計誤差」などと呼ぶことにする．
**しかし我々は，$F^{-1}(u)$を$r(u)$で近似することにより，$E_{N}$をさらに$\hat{E}_{N}(r)$ を使って二段階にわたる近似をすることを考えている．つまり，考慮すべき全体の誤差としては$|E - \hat{E}_{N}(r)|$ となる**．
ここで，三角不等式を使うと，以下のように分解できる

$$ |E - \hat{E}_N(r)| \le \underbrace{|E - E_N|}_{\text{モンテカルロの誤差}} + \underbrace{|E_N - \hat{E}_N(r)|}_{\text{モデルの近似誤差}} = \mathcal{O}(\log N/N \text{または } 1/N) + \underbrace{|E_N - \hat{E}_N(r)|}_{\text{モデルの近似誤差}}$$
具体的な$N$の値として，実務で使うのは高々$N = 10^7$  であると仮定する．すると，モンテカルロの誤差の方は$10^{-7}$ から$10^{-6}$ 程度となる．これは原理的な誤差であり，これの意味するところは，(素朴にSobol'列のような準乱数列を使う限りは) 
**「全体の誤差として，$10^{-7}$ から$10^{-6}$ の誤差は，(モデルの近似誤差をどんなに小さくしても)原理的に避けられない」** 
ということである．これはつまり，**モデルの近似誤差が$10^{-7}$ のオーダー程度の小ささであれば，モンテカルロの誤差に「埋もれる」ため，$10^{-7}$ 程度小さければ十分**ということである．次に，モデルの近似誤差を詳しく評価すると
$$
\begin{align*}
\underbrace{|E_N - \hat{E}_N(r)|}_{\text{モデルの近似誤差}} &= \frac{1}{N}| \sum_{i=1}^{N}( g(F^{-1}(u_{i})) - g(r(u_{i})) )| \\
&\le \frac{1}{N} \sum_{i=1}^{N}| g(F^{-1}(u_{i})) - g(r(u_{i})) | \\
&\le \frac{L}{N} \sum_{i=1}^{N}|F^{-1}(u_{i}) - r(u_{i})| \\
&\le \frac{L}{N} \times N \|F^{-1} -r\|_{\infty} = L\|F^{-1} -r\|_{\infty}
\end{align*}
$$

ただし，   $L := \sup_x \vert{}g'(x)\vert{}$ はリプシッツ定数である．$L$はヨーロピアンオプションのような一般的なペイオフ関数の場合、オーダーを変えるほどではない (バリアオプションなどでは少し話が変わってくる)ので，そのような場合を仮定する． これにより，モデルの近似誤差には最大絶対誤差$\|F^{-1} - r\|_{\infty}$の値が直接効いてくる．　よって，$\|F^{-1} - r\|_{\infty}$ が$10^{-7}$ 程度のオーダーであれば，モデルの近似誤差も$10^{-7}$ 程度に収まる．
